---
phase: 01
plan: 3
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/rapporteur_helper/itut/work_programme.py
  - src/rapporteur_helper/itut/questions.py
  - tests/test_itut_parsing.py
autonomous: true
requirements:
  - REQ-TECH-001
  - REQ-TECH-002
  - REQ-TECH-003
must_haves:
  truths:
    - "itu_t scraper explicitly targets IndexError/AttributeError instead of bare Exceptions."
    - "Test case securely mocks the ITU-T live site using responses."
  artifacts:
    - path: "src/rapporteur_helper/itut/work_programme.py"
      provides: "Hardened parsing rules."
    - path: "tests/test_itut_parsing.py"
      provides: "Test suite verifying parser logic."
  key_links:
    - from: "tests/test_itut_parsing.py"
      to: "src/rapporteur_helper/itut/work_programme.py"
      via: "imports"
      pattern: "from .*itut.* import"
---
<objective>
To harden the core ITU-T data scraper by eliminating bare exceptions and adding integration tests.

Purpose: Avoids silent dataloss during standard website template updates by raising clear parsing exceptions and logging correctly.
Output: Refactored `itut` modules and `test_itut_parsing.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fix exceptions and add TypedDict for Work Programme</name>
  <read_first>src/rapporteur_helper/itut/work_programme.py, src/rapporteur_helper/itut/questions.py, RESEARCH.md (Pitfalls)</read_first>
  <files>src/rapporteur_helper/itut/work_programme.py, src/rapporteur_helper/itut/questions.py</files>
  <action>Open `work_programme.py` and `questions.py`. Search for `except Exception:` and `except Exception as e:`. Replace them with specific handling classes (like `IndexError, AttributeError, ValueError`). Introduce `logging.getLogger(__name__)` and replace silent `pass` blocks with `logger.warning("Parsing failed", exc_info=True)`. Also update `get_work_program` adding strict return types `list[WorkItem]` where `WorkItem` is a newly declared `TypedDict`.</action>
  <verify>
    <automated>uv run python -c "import pathlib, sys; paths=['src/rapporteur_helper/itut/work_programme.py', 'src/rapporteur_helper/itut/questions.py']; sys.exit(1 if any('except Exception:' in pathlib.Path(p).read_text() for p in paths) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `except Exception:` blocks removed from `itut/work_programme.py` and `itut/questions.py`.
    - `logger.warning` is correctly implemented where `pass` existed.
  </acceptance_criteria>
  <done>ITUT exception squashing removed.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for itut parsing</name>
  <read_first>tests/data/workprog_sample.html</read_first>
  <files>tests/test_itut_parsing.py</files>
  <action>Create `tests/test_itut_parsing.py`. Utilize the `responses` package (`@responses.activate`) to mock `requests.get` returning the static `tests/data/workprog_sample.html`. Invoke `get_work_program` and assert it parses correctly into mocked `TypedDict` arrays without triggering live network calls.</action>
  <verify>
    <automated>uv run python -m pytest tests/test_itut_parsing.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_itut_parsing.py` passes.
    - Zero outside HTTP requests executed during tests.
  </acceptance_criteria>
  <done>Tests pass on ITUT.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network | Ensure responses blocks outgoing GET requests to `itu.int`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03   | Tampering | `test_itut_parsing.py` | mitigate | Use `@responses.activate` wrapper. |
</threat_model>

<verification>
`grep` confirms absent `except Exception`. Pytest checks test suites.
</verification>

<success_criteria>
- No bare `except Exception:` present in the target files.
- Pytest offline mock operates correctly.
</success_criteria>

<output>
Create `.planning/phases/01/01-03-SUMMARY.md` when done
</output>