---
phase: 01
plan: 2
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/rapporteur_helper/word_docx/links.py
  - src/rapporteur_helper/word_docx/tables.py
  - tests/test_word_docx.py
autonomous: true
requirements:
  - REQ-TECH-001
  - REQ-TECH-002
must_haves:
  truths:
    - "Developers correctly receive static type checking errors if they pass invalid formatting arguments to docx utilities."
    - "Docx processing functions execute correctly without producing unexpected disk IO or crashing."
  artifacts:
    - path: "src/rapporteur_helper/word_docx/links.py"
      provides: "Hyperlink functions with updated types."
    - path: "tests/test_word_docx.py"
      provides: "Tests for docx modifications."
  key_links:
    - from: "tests/test_word_docx.py"
      to: "src/rapporteur_helper/word_docx/links.py"
      via: "imports"
      pattern: "from .*word_docx.* import"
---
<objective>
To modernize and test the `word_docx` modules.

Purpose: Cleans up typing anti-patterns (such as mutable list defaults) and verifies formatting operations without disk IO.
Output: Refactored `links.py`, `tables.py` and new `test_word_docx.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Refactor Word Docx Types</name>
  <read_first>src/rapporteur_helper/word_docx/links.py, src/rapporteur_helper/word_docx/tables.py, RESEARCH.md (Typing and Coding Standards)</read_first>
  <files>src/rapporteur_helper/word_docx/links.py, src/rapporteur_helper/word_docx/tables.py</files>
  <action>Open `links.py` and `tables.py`. Find default list instantiations used for type pseudo-enums (e.g. `format=["None", "bold"][0]`). Replace them with `typing.Literal` definitions from Python array annotations (e.g. `format: Literal["None", "bold"] = "None"`). Add return types (`None` or explicit `docx.Document` element) to functions like `create_hyperlink` and `replace_in_table`.</action>
  <verify>
    <automated>uv run ty check</automated>
  </verify>
  <acceptance_criteria>
    - `links.py` utilizes `Literal` instead of list indices for `format` arguments.
    - Type annotations added completely in both files.
  </acceptance_criteria>
  <done>Types refactored.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for word_docx</name>
  <read_first>src/rapporteur_helper/word_docx/links.py</read_first>
  <files>tests/test_word_docx.py</files>
  <action>Create `tests/test_word_docx.py`. Import `docx.Document`, construct an in-memory document with a generic paragraph and table. Call `create_hyperlink` and `replace_in_table` from `rapporteur_helper.word_docx`. Assert that the text elements contain the proper hyperlinks or replaced texts. There must be no disk IO output.</action>
  <verify>
    <automated>uv run python -m pytest tests/test_word_docx.py</automated>
  </verify>
  <acceptance_criteria>
    - Passes `pytest tests/test_word_docx.py`.
    - Function executes without file side effects.
  </acceptance_criteria>
  <done>Tests pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IO/Disk | `word_docx` text parsing in memory rather than untrusted local files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02   | Tampering | `test_word_docx.py` | accept | Document manipulation rests entirely within isolated unit tests. |
</threat_model>

<verification>
Automated pytest runs confirm test validity.
</verification>

<success_criteria>
- Type checks pass cleanly for the docx component.
- All docx unit tests pass with in-memory validation.
</success_criteria>

<output>
Create `.planning/phases/01/01-02-SUMMARY.md` when done
</output>