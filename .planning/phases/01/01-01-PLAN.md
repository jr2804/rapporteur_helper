---
phase: 01
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/data/workprog_sample.html
  - DEV_GUIDE.md
autonomous: true
requirements:
  - REQ-TECH-001
  - REQ-TECH-002
must_haves:
  truths:
    - "Test stub data provides a valid ITU-T HTML representation."
    - "Developers are instructed on how to run tests and pre-commit hooks."
  artifacts:
    - path: "pyproject.toml"
      provides: "New dependency declarations."
      contains: "responses"
    - path: "tests/conftest.py"
      provides: "Test scaffolding."
    - path: "DEV_GUIDE.md"
      provides: "Developer test instructions."
  key_links:
    - from: "tests/conftest.py"
      to: "pyproject.toml"
      via: "pytest execution"
      pattern: "pytest"
---
<objective>
To establish the technical baseline, test dependencies, and necessary mocking scaffolds.

Purpose: Creates the testing environment required before refactoring error handling in subsequent plans.
Output: Configured `pyproject.toml`, test stubs, and developer guide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add dependencies and test scaffolding</name>
  <read_first>pyproject.toml, RESEARCH.md (Deps section)</read_first>
  <files>pyproject.toml, tests/conftest.py, tests/data/workprog_sample.html</files>
  <action>Open `pyproject.toml` and manually add `lxml>=5.3.0` to the `dependencies` list. Add `responses>=0.25.0` to the `dev` dependency-group list, matching existing formatting. Do not use 'uv add' since we edit files via code. Create `tests/conftest.py` with standard `pytest` initializations. Create `tests/data/workprog_sample.html` with a minimal valid dummy HTML snippet describing an ITU-T work program (e.g. valid `<table>` with columns headers suitable for scraping). Then, execute `uv sync` to ensure the dependencies are installed into the environment.</action>
  <verify>
    <automated>uv pip show lxml responses</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` includes `lxml` in `dependencies`.
    - `pyproject.toml` includes `responses` in `dev` dependency-group.
    - `tests/data/workprog_sample.html` exists and contains basic `<html>` tags.
  </acceptance_criteria>
  <done>Dependencies and stubs injected successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Create DEV_GUIDE.md</name>
  <read_first>.planning/STATE.md</read_first>
  <files>DEV_GUIDE.md</files>
  <action>Create a `DEV_GUIDE.md` file in the project root. Document how to run the tests via `uv run python -m pytest`, how to run tox `uv run tox`, and how pre-commit hooks run `ruff` and `ty check`. Use MARKDOWN.</action>
  <verify>
    <automated>pwsh -Command "if (Test-Path 'DEV_GUIDE.md') { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `DEV_GUIDE.md` correctly states testing and formatting execution commands.
  </acceptance_criteria>
  <done>DEV_GUIDE.md created.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing API | Stubbing requests prevents outgoing calls to ITU-T |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01   | Information | `tests/conftest.py` | mitigate    | Ensure `responses` mocks block real network requests during tests. |
</threat_model>

<verification>
File existence checks and package query.
</verification>

<success_criteria>
- pyproject.toml dependencies updated.
- Test stub exists.
- DEV_GUIDE.md is present.
</success_criteria>

<output>
Create `.planning/phases/01/01-01-SUMMARY.md` when done
</output>