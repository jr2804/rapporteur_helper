---
phase: 01
plan: 4
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/rapporteur_helper/html/__init__.py
  - src/rapporteur_helper/content/documents.py
  - src/rapporteur_helper/generate_reports.py
autonomous: true
requirements:
  - REQ-TECH-001
  - REQ-TECH-004
must_haves:
  truths:
    - "Application generation steps properly trace exception classes instead of swallowing core faults."
  artifacts:
    - path: "src/rapporteur_helper/html/__init__.py"
      provides: "Strict error bubbling for HTML requests."
    - path: "src/rapporteur_helper/content/documents.py"
      provides: "Structured reporting logic exceptions."
  key_links: []
---
<objective>
To clean up remaining bare `except` blocks across HTML handlers, documents, and report generators.

Purpose: Finalizes the codebase audit for error handling regressions against anti-patterns noted in the technical debt map.
Output: Refactored exception logic in outstanding files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fix exceptions in auxiliary handlers</name>
  <read_first>src/rapporteur_helper/html/__init__.py, src/rapporteur_helper/content/documents.py, src/rapporteur_helper/generate_reports.py</read_first>
  <files>src/rapporteur_helper/html/__init__.py, src/rapporteur_helper/content/documents.py, src/rapporteur_helper/generate_reports.py</files>
  <action>Open `__init__.py` under `html/`, `documents.py` under `content/` and `generate_reports.py` at the root module. Grep and inspect for instances of `except Exception:` and `except Exception as e:`. Convert network calls (like `html/`) to explicitly catch `requests.RequestException`. Convert generation logic to catch specific template/IO bounds (e.g. `OSError, ValueError`). Add explicit standard library `logging.warning()` if continuing, or `raise` with specificity.</action>
  <verify>
    <automated>uv run python -c "import pathlib, sys; paths=['src/rapporteur_helper/html/__init__.py', 'src/rapporteur_helper/content/documents.py', 'src/rapporteur_helper/generate_reports.py']; sys.exit(1 if any('except Exception:' in pathlib.Path(p).read_text(encoding='utf-8') for p in paths) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `except Exception:` strings no longer exist in the specified files.
    - Handled classes correctly narrow scope to logical errors.
  </acceptance_criteria>
  <done>Remaining core exceptions mitigated.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File parsing | Ensures DOCX generation accurately reflects failure scopes to the CLI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04   | Denial of Service | `documents.py` | mitigate | Bubble logical file issues upwards instead of hanging into silent passes. |
</threat_model>

<verification>
`grep` confirms absent `except Exception`. Validation across `uv run ty check` completes without issues for updated block scoping.
</verification>

<success_criteria>
- No bare `except Exception:` present.
- Logging statements encapsulate variables safely.
</success_criteria>

<output>
Create `.planning/phases/01/01-04-SUMMARY.md` when done
</output>